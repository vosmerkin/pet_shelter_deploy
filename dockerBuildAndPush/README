1. put docker-build.yaml to .github/workflows/docker-build.yml
2. put Dockerfile to project root, edit the file - change *.jar name according to the project name
2. Configure registry access (add secrets to Github for Docker registry access)
